server:
  port: 8003

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:XE
    username: RMS
    password: hari2005
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.OracleDialect
    properties:
      hibernate:
        format_sql: true
        dialect:
          oracle:
            version: 11
  servlet:
    multipart:
      max-file-size: 5MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: stylishhari2005@gmail.com
    password: "nprg sbmj ubaa eakr"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/

app:
  jwt:
    secret: SquareFeetX_Super_Secret_Key_2026_For_JWT_Token_Generation_Must_Be_Long_Enough
    access-token-expiry: 900000
    refresh-token-expiry: 604800000
  cloudinary:
    cloud-name: squarefeetx
    api-key: YOUR_API_KEY
    api-secret: YOUR_API_SECRET
